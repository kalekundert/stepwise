[pytest]
#addopts = --cov stepwise --cov-report html --no-cov-on-fail
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
filterwarnings =
    ignore::PendingDeprecationWarning:Bio.Alphabet
